let number = document.getElementById('number');

// console.log(number);
number.replace(/ /g,"");

function multiply(x , y){
    return (x * y);
}
function divide(x, y){
    return (x / y);
}
function add(x, y){
    return (x + y);
}
function remove(x, y){
    return (x - y);
}